import { Button, styled } from '@mui/material';

const RowModalLayoutButton = styled(Button)(({ theme }) => ({
  paddingX: '1.5rem',
  borderRadius: '1rem',
}));

export default RowModalLayoutButton;
